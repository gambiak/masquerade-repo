import { getCurrentUser } from "@/lib/auth";
import { query } from "@/lib/db";
import { redirect,notFound } from "next/navigation";
export default async function JoinCrew({params}:{params:Promise<{code:string}>}){const {code}=await params;const user=await getCurrentUser();if(!user)redirect(`/login?returnTo=/join/${encodeURIComponent(code)}`);const crew=(await query<any>(`select * from crews where invite_code=$1`,[code])).rows[0];if(!crew)notFound();const joined=(await query<any>(`select 1 from crew_members where crew_id=$1 and user_id=$2`,[crew.id,user.id])).rows[0];return <main><section className="hero"><div className="eyebrow">Morning Crew</div><h1>{crew.name}</h1><p>Private daily Masquerade group.</p></section><section className="card">{joined?<><h2>You're in.</h2><a className="btn primary" href="/crew">OPEN CREWS</a></>:<form action="/api/crew/join" method="post"><input type="hidden" name="code" value={code}/><button className="btn primary">JOIN CREW</button></form>}</section></main>}
