import { getCurrentUser } from "@/lib/auth";import { query } from "@/lib/db";import { redirect } from "next/navigation";
export default async function Stats(){const user=await getCurrentUser();if(!user)redirect("/login");const runs=(await query<any>(`select * from game_sessions where user_id=$1 and status='completed' order by completed_at desc limit 30`,[user.id])).rows;const games=runs.length,avg=games?Math.round(runs.reduce((a,x)=>a+Number(x.score||0),0)/games):0,pure=runs.reduce((a,x)=>a+Number(x.pure_solves||0),0);return <main><section className="hero"><div className="eyebrow">Your mind</div><h1>Pattern Hunter.</h1><p>Game-performance indicators, not cognitive or medical assessments.</p></section><div className="stats"><div className="stat"><b>{games}</b><span>games</span></div><div className="stat"><b>{avg}</b><span>avg score</span></div><div className="stat"><b>{pure}</b><span>Pure Solves</span></div><div className="stat"><b>{runs[0]?.hints_used||0}</b><span>latest hints</span></div></div></main>}
